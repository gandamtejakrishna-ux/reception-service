play.filters.enabled = [
  "play.filters.hosts.AllowedHostsFilter"
]

play.filters.disabled += "play.filters.csrf.CSRFFilter"


# https://www.playframework.com/documentation/latest/Configuration
booking.service.url = "http://localhost:9002"
play.server.http.port = 9001

auth {
  username = "reception"
  password = "pass123"
}

# Optional: request timeout and retry configuration
booking.service.requestTimeoutMs = 5000
booking.service.maxRetries = 2
booking.service.retryDelayMs = 300
jwt {
  secret = "hotel-secret-key-1234567890"
  expiry = 3600  # seconds
}

